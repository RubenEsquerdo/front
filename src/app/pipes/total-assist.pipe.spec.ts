import { TotalAssistPipe } from './total-assist.pipe';

describe('TotalAssistPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalAssistPipe();
    expect(pipe).toBeTruthy();
  });
});
