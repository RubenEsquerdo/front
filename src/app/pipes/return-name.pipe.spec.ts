import { ReturnNamePipe } from './return-name.pipe';

describe('ReturnNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnNamePipe();
    expect(pipe).toBeTruthy();
  });
});
